const CardFavorites = ({
  id,
  name,
  image,
  price,
  age,
  status,
  category,
  brand,
  description,
}) => {
  return (
    <div>
      <p>{name}</p>
    </div>
  );
};

export default CardFavorites;
